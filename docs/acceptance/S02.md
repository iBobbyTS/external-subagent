# S02 ZCode 生命周期验收映射

本文件只记录恢复测试与 owner 的对应关系；运行结果以当前工作树测试输出和父级 S02.B review 为准。

| 验收面 | 测试入口 | owner | 当前合同 |
| --- | --- | --- | --- |
| bounded diagnose、daemon identity、日志截断/轮转、目标 agent 诊断 | `tests/cli/diagnose.test.mjs` | CLI diagnose / RPC projection | 只读公共 wait 投影；日志读取有文件白名单和字节上限 |
| backup/restore、损坏校验、uninstall/purge、legacy cleanup | `tests/cli/maintenance.test.mjs` | CLI maintenance / product paths | manifest product 为 `external-subagent`；恢复前完整校验；uninstall 保留 data |
| wait/result/list/observe/send/错误与协议帧边界 | `tests/cli/rpc.test.mjs` | CLI RPC facade | RPC v13、agent id 10000000–99999999、2 MiB 响应帧上限、256 KiB result page |
| 公开 MCP 工具目录与投影字段 | `tests/cli/public-api-schema.test.mjs` | public schema / MCP contract | 对外工具使用 `external_subagent_*`；隐藏内部字段与 source-control 元数据 |
| macOS dry-run 与非 macOS 生命周期拒绝 | `tests/platform/macos.test.mjs`, `tests/platform/windows.test.mjs` | CLI platform gate | dry-run 无副作用；非 darwin 业务命令结构化返回 `UNSUPPORTED_PLATFORM` |

## 来源与排除

- 来源为只读仓库 `/Users/ibobby/Projects/zcode-mcp` 固定 HEAD `bb45d562671ddbd99637c5680449bc75aedb378b`。
- 迁移时不复制 credentials、workspaces、`.git`、缓存、压缩产物或 `.DS_Store`。
- 旧文件名只作为 legacy cleanup 的被清理对象；新产品公开身份保持 `external-subagent`。
