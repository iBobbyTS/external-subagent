# S01 acceptance record

Status: **BLOCKED / NOT_RUN (live)**; local fixture checks pass.

The bounded probe records executable path, argv, timestamp, JSON-RPC responses, redacted stderr, malformed-frame count, exit code, and signal. It keeps stdout wire data separate from diagnostics and applies a deadline with SIGTERM cleanup. The fixture demonstrates model catalog discovery before prompt, permission update correlation (`requestId`/`toolCallId`), message identity, cancellation, and EOF handling.

Live ACP evidence is not available in this environment: no authorized DSH executable/version, provider credential, or disposable real workspace was provided. Consequently the following S01 oracles remain unverified: real initialize/capabilities, catalog token acceptance, auth failure versus ACP handshake, hi settlement, strict plan refusal of shell/indirect execution/write, native permission allow/deny, and runtime cleanup under malformed/blocked streams.

Acceptance decision: retain discovery/probe artifacts only. Do not advertise DSH spawn support and do not add `plugins/dsh-policy/` until a narrow, evidence-backed decision and plan delta exists.
