[package]
name = "external-agent-dsh"
version = "0.1.2"
edition = "2021"
license.workspace = true

[dependencies]
external-contract = { path = "../../external-contract" }
external-runtime = { path = "../../external-runtime" }
serde_json = "1"
serde_yaml = "0.9"

[target.'cfg(unix)'.dependencies]
libc = "0.2"
